name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: checkout master
      uses: actions/checkout@v2
    - name: build pgg
      run: |
       mkdir bin
       make clean all
